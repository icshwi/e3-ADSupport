## Please DO NOT CHANGE the following options.
## It is not easy to handle all dependency libraries with all possible options.
## We have to use few limited options for ADSupport.
## If one needs more complicated one, please contact han.lee@esss.se
##
##
WITH_NETCDF:=YES
NETCDF_EXTERNAL:=YES
##
##
##
WITH_JPEG:=YES
## We can use ONLY JPEG_EXTERNAL:=YES NOW
##
JPEG_EXTERNAL:=YES
#JPEG_EXTERNAL:=NO
##
##
##
WITH_ZLIB:=YES
##
##
##
# # BLOSC depends on ZLIB, so WITH_ZLIB:=YES
WITH_BLOSC:=YES
## IFC14XX, no SSE2
## BLOSC_SSE2:=YES
## BLOSC_AVX2:=YES
## BLOSC_EXTERNAL:=YES
##
##
##
# TIFF depends on ZLIB, so WITH_ZLIB:=YES
# WITH_TIFF cannot be real variable due to unknown reasons
# So, have to change it to WITH_TIFF0, which is also defined
# in ADSupport.Makefile
#
WITH_TIFF0:=YES
TIFF_EXTERNAL:=YES
##
##
##
## We don't use XML2 in ADSupport
## USE the system library libxml2
##
##
##
WITH_SZIP:=YES
##
##
##
WITH_HDF5:=YES
HDF5_EXTERNAL:=YES
##
##
WITH_BITSHUFFLE:=YES
##
##
##
WITH_NEXUS:=YES
##
##
##

# The definitions shown below can also be placed in an untracked CONFIG_OPTIONS.local
-include $(TOP)/configure/CONFIG_OPTIONS.local

# It can be removed one year later, because _EXTERNAL was added to require global
EXPORT_VARS+=$(filter %_EXTERNAL%,$(.VARIABLES))
EXPORT_VARS+=BLOSC_SSE2
EXPORT_VARS+=BLOSC_AVX2